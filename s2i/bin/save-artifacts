#!/bin/sh -e

echo "Hola"
#Spushd ${HOME} >/dev/null

# ${HOME}/.m2 is the maven repo dir
# {$HOME}/target is the class files from the previous build
#Sif [ -d ./target ]; then
#S    tar cf - ./.m2 ./target
#Selse
#S    tar cf - ./.m2
#Sfi

#Spopd >/dev/null
